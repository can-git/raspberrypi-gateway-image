# Copy this file to "wifi.env" (same folder) and fill in the real values.
# wifi.env is gitignored so the WiFi password never gets committed.
# In CI, create wifi.env from repository secrets before the build.
WIFI_SSID=YourSSID
WIFI_PSK=YourWifiPassword
