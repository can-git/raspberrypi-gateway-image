# WiFi credentials baked into the image at build time — never commit the real values.
step/07-network/wifi.env

# Bootstrap (raspi-provision) private key — see step/08-default-cert/README.
step/08-default-cert/files/device-key.pem

# Python bytecode
__pycache__/
*.pyc

# Materialized at build time from nu-gateway-service (Bitbucket) - never commit.
step/06-gateway-service/files/nugw.tar
step/10-nu-provision/files/provision/
